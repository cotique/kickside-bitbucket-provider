version: "1.0"
namespace: cotique.bitbucket_provider.api

entries:
  # cotique.bitbucket_provider.api:get_status
  - name: get_status
    kind: function.lua
    meta:
      comment: GET /bitbucket-provider/status — static module identity and capability snapshot for the bitbucket-provider page.
    source: file://get_status.lua
    method: handler
    modules:
      - http
      - security

  # cotique.bitbucket_provider.api:get_status.endpoint
  - name: get_status.endpoint
    kind: http.endpoint
    meta:
      comment: Status endpoint behind the authenticated API router.
      router: app:api
    method: GET
    func: get_status
    path: /bitbucket-provider/status
